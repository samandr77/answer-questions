# 🚀 API-сервис для вопросов и ответов

Простой REST API для управления вопросами и ответами, построенный на Go с использованием стандартной библиотеки net/http, GORM и PostgreSQL.

[![Go Версия](https://img.shields.io/badge/go-%3E%3D1.24-blue.svg)](https://golang.org/)
[![Docker](https://img.shields.io/badge/Docker-24.0+-blue.svg)](https://www.docker.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15+-336791.svg)](https://www.postgresql.org/)

## ❓ Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://gitlab.net.buroburo.tech/russ/answer-questions.git
cd answer-questions
```

### 2. Запустите все сервисы (БД + приложение)

```bash
make up-docker
```

Эта команда:
- Пересобирает Docker образ приложения
- Запускает PostgreSQL 15
- Применяет миграции БД
- Запускает HTTP сервер на http://localhost:8080

### 3. Проверьте статус сервисов

```bash
make ps
```

### 4. Тестируйте API

```bash
# Создать вопрос
curl -X POST http://localhost:8080/questions/ \
  -H "Content-Type: application/json" \
  -d '{"text": "Как выучить Go?"}'

# Получить все вопросы
curl http://localhost:8080/questions/
```

### 5. Остановите сервисы

```bash
make down
```

## 🌟 Ключевые возможности

- **REST API** для управления вопросами и ответами
- **Валидация данных** на уровне сервиса
- **Каскадное удаление** ответов при удалении вопроса
- **Миграции БД** с помощью goose
- **Docker** контейнеризация для простого развертывания

## 🏗 Архитектура

```
┌──────────────────────────────────────────────────┐
│           HTTP Handlers (API Layer)              │
│  GET /questions/, POST /questions/               │
│  GET /questions/{id}, DELETE /questions/{id}     │
│  POST /questions/{id}/answers/                   │
│  GET /answers/{id}, DELETE /answers/{id}         │
└────────────┬─────────────────────────────────────┘
             │
┌────────────▼─────────────────────────────────────┐
│         Service Layer (Бизнес-логика)            │
│  - Валидация вопросов и ответов                  │
│  - Проверка существования вопроса               │
│  - Каскадное удаление ответов                   │
└────────────┬─────────────────────────────────────┘
             │
┌────────────▼─────────────────────────────────────┐
│    Repository Layer (Доступ к данным - GORM)    │
│  - QuestionRepository                            │
│  - AnswerRepository                              │
└────────────┬─────────────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
┌───▼──────────┐
│ PostgreSQL   │
│ (Single DB)  │
└──────────────┘
```

## 🔄 API Endpoints

### Questions (Вопросы)

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/questions/` | Получить все вопросы |
| POST | `/questions/` | Создать новый вопрос |
| GET | `/questions/{id}` | Получить вопрос с ответами |
| DELETE | `/questions/{id}` | Удалить вопрос (каскадно) |

### Answers (Ответы)

| Метод | Endpoint | Описание |
|-------|----------|---------|
| POST | `/questions/{id}/answers/` | Добавить ответ к вопросу |
| GET | `/answers/{id}` | Получить конкретный ответ |
| DELETE | `/answers/{id}` | Удалить ответ |

## 🔄 Бизнес-логика

### Правила работы

- Нельзя создать ответ к несуществующему вопросу
- Один и тот же пользователь может оставлять несколько ответов на один вопрос
- При удалении вопроса автоматически удаляются все его ответы (каскадно через ON DELETE CASCADE)

### Создание вопроса

```
Client → HTTP Handler → Service Layer → Repository → PostgreSQL
```

### Добавление ответа

```
Client → HTTP Handler → Service Layer (проверка вопроса) → Repository → PostgreSQL
```

### Удаление вопроса

```
Client → Service Layer → Repository → PostgreSQL (CASCADE удаление ответов)
```

## 📊 Схема базы данных

### Таблицы

**questions**
```sql
CREATE TABLE questions (
  id SERIAL PRIMARY KEY,
  text VARCHAR(1000) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**answers**
```sql
CREATE TABLE answers (
  id SERIAL PRIMARY KEY,
  question_id INTEGER NOT NULL REFERENCES questions(id) ON DELETE CASCADE,
  user_id VARCHAR(255) NOT NULL,
  text VARCHAR(1000) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 🛠 Стек технологий

### Ядро

- **Язык**: Go 1.24+
- **Web фреймворк**: net/http (стандартная библиотека)
- **БД**: PostgreSQL 15
- **ORM**: GORM

### Инфраструктура

- **Контейнеризация**: Docker
- **Оркестрация**: Docker Compose
- **Миграции БД**: goose

### Разработка

- **Тестирование**: httptest, testify
- **Качество кода**: golangci-lint
- **Управление версиями**: Git

## 📦 Структура проекта

```
answer-questions/
├── cmd/app/
│   └── main.go                      # Точка входа приложения
├── internal/
│   ├── api/
│   │   ├── handler.go               # HTTP обработчики
│   │   ├── router.go                # Определение маршрутов
│   │   ├── middleware.go            # HTTP middleware
│   │   └── dto.go                   # Request/Response DTO
│   ├── entity/
│   │   ├── question.go              # Domain модель Question
│   │   ├── answer.go                # Domain модель Answer
│   │   └── errors.go                # Domain ошибки
│   ├── repository/
│   │   ├── question_repo.go         # Repository вопросов
│   │   ├── answer_repo.go           # Repository ответов
│   │   └── db.go                    # Интерфейсы репозиториев
│   ├── service/
│   │   ├── question_service.go      # Логика вопросов
│   │   ├── answer_service.go        # Логика ответов
│   │   └── validator.go             # Валидация данных
│   └── config/
│       └── config.go                # Конфигурация из переменных окружения
├── migrations/
│   ├── 20251204100000_init_questions_table.sql
│   └── 20251204100001_init_answers_table.sql
├── docker/
│   ├── docker-compose.yml          # Окружение локальной разработки
│   └── Dockerfile                  # Образ контейнера
├── Makefile                         # Команды разработки
├── go.mod                           # Go модули
└── README.md                        # Документация проекта
```

## 🚀 Начало работы

### 1. Требования

```bash
# Проверьте версию Go
go version  # >= 1.24

# Проверьте Docker
docker --version
docker-compose --version
```

### 2. Клонируйте репозиторий

```bash
git clone https://gitlab.net.buroburo.tech/russ/answer-questions.git
cd answer-questions
```

### 3. Запустите сервисы

```bash
# Запустите все сервисы (БД + приложение + миграции)
make up-docker

# Проверьте статус контейнеров
make ps
```

### 4. Проверьте здоровье

```bash
# Тест API
curl http://localhost:8080/questions/

# Ожидаемый ответ
{"questions": []}
```

## 💻 Команды разработки

### Основные команды

```bash
# Запустить приложение локально (нужна БД на localhost:5432)
make run

# Запустить только БД
make up

# Запустить оба контейнера (БД + приложение) с пересборкой
make up-docker

# Остановить все контейнеры
make down

# Показать статус контейнеров
make ps
```

### Команды БД

```bash
# Создать новую миграцию
make migrate-new name=add_categories

# Применить миграции (требует БД на localhost:5432)
make migrate-up

# Откатить миграции
make migrate-down
```

### Команды тестирования

```bash
# Запустить все тесты с изолированной БД
# Автоматически поднимает тестовую БД на порту 15432
# Применяет миграции и запускает тесты с флагом -race
make test

# Очистить тестовую БД
make test-down
```

### Команды кода

```bash
# Запустить linter
make lint
```

## 📊 Примеры API

### Создать вопрос

```bash
curl -X POST http://localhost:8080/questions/ \
  -H "Content-Type: application/json" \
  -d '{"text": "Как выучить Go?"}'

# Ответ (201 Created)
{
  "id": 1,
  "text": "Как выучить Go?",
  "created_at": "2025-12-04T10:30:00Z"
}
```

### Получить все вопросы

```bash
curl http://localhost:8080/questions/

# Ответ (200 OK)
{
  "questions": [
    {
      "id": 1,
      "text": "Как выучить Go?"
    }
  ]
}
```

### Получить вопрос с ответами

```bash
curl http://localhost:8080/questions/1

# Ответ (200 OK)
{
  "id": 1,
  "text": "Как выучить Go?",
  "created_at": "2025-12-04T10:30:00Z",
  "answers": []
}
```

### Удалить вопрос

```bash
curl -X DELETE http://localhost:8080/questions/1

# Ответ (204 No Content)
# Автоматически удаляются все ответы (CASCADE)
```

## 🔐 Функции безопасности

- ✅ **Валидация входных данных** - проверка текста вопроса и ответа
- ✅ **SQL Injection prevention** - использование параметризованных запросов (GORM)
- ✅ **CORS конфигурация** - контроль кросс-доменных запросов
- ✅ **Error handling** - безопасные сообщения об ошибках

## 🐛 Устранение неполадок

### Сервис не запускается

```bash
# Проверьте статус контейнеров
make ps

# Проверьте логи приложения
docker logs questions_app

# Проверьте логи БД
docker logs questions_postgres

# Пересоберите и запустите
make up-docker
```

### Проблемы БД

```bash
# Проверьте статус контейнеров
make ps

# Проверьте PostgreSQL
docker exec questions_postgres psql -U postgres -d questions_db -c "SELECT 1"

# Проверьте логи
docker logs questions_postgres

# Примените миграции вручную
make migrate-up
```

### Требования к коду

- Следуйте Go соглашениям
- Пишите unit тесты
- Запустите `make lint`
- Обновите документацию

---
